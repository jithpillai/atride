# PostgreSQL/PostGIS is introduced when the persistence adapter replaces seed data.
DATABASE_URL="postgresql://atride:atride@localhost:5432/atride"

# Providers remain mocked during the first UI milestone.
EMAIL_PROVIDER="mock"
SMS_PROVIDER="mock"
